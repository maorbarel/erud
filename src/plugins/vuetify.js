import Vue from 'vue';
import Vuetify from 'vuetify/lib';

Vue.use(Vuetify);

export default new Vuetify({
   theme: {
      themes: {
        light: {
          eruditeGreen:'#00C876',
          eruditePurple:'#014C81',
        }
      }
    }
});